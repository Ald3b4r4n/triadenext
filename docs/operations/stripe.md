# Stripe

Stripe real permanece desativado nesta fundacao.

`src/app/api/webhooks/stripe/route.ts` retorna placeholder controlado. A implementacao real deve seguir `docs/migration/parity-test-plan.md`, validando assinatura e idempotencia.
