# Spacing

## Tokens legados

- page: `clamp(1rem, 2.6vw, 2rem)`.
- section: `clamp(2.5rem, 6vw, 5rem)`.

## Tokens Next atuais

- page-shell: `min(1120px, calc(100% - 32px))`.
- padding vertical padrão: `48px`.
- seção storefront: `88px` a `96px`.
- hero desktop: `72px`.
- hero mobile: `64px 0 80px`.

## Raios

- card legado: `8px`.
- control legado: `6px`.
- pill: `999px`.
- card Next público: `18px`.

## Direção

Manter grid mobile-first, `page-shell` de 1120px e espaçamento generoso em storefront. Admin deve ser mais denso e usar raios menores.
